<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head', {title: 'Formulario de inicio de sesión'}) %>
<body>
    
    <!-- Header -->

    <%- include('partials/header') %>
    <div class="container pb-5 container-signIn">
        <form class="px-4 py-3 signIn-form" action="" method="POST">
            <h1>Iniciar Sesión:</h1>
          <div class="form-group">
            <label class="signIn-form-label" for="username">Nombre de usuario:</label>
            <input type="text" class="form-control" id="signIn-form-input" name="username">
            <small><%= (typeof errors != 'undefined' && errors.username) ? errors.username.msg : '' %></small>
            <small><%= (typeof errors != 'undefined' && errors.usernamePass) ? errors.usernamePass.msg : '' %></small>
          </div>
          <div class="form-group">
            <label class="signIn-form-label" for="password">Contraseña:</label>
            <input type="password" class="form-control" id="signIn-form-input" name="password">
            <small><%= (typeof errors != 'undefined' && errors.password) ? errors.password.msg : '' %></small>
            <small><%= (typeof errors != 'undefined' && errors.usernamePass) ? errors.usernamePass.msg : '' %></small>
          </div>
          <div class="form-group">
            <div class="form-check">
              <input type="checkbox" class="form-check-input" id="dropdownCheck" name="remember">
              <label class="form-check-label" for="dropdownCheck">
                Recordarme
              </label>
            </div>
          </div>
          <div style="float:right;">
              <button class="signIn-form-button" type="submit">Iniciar Sesión</button>
          </div>
          <br><br><br>
        </form>
        <div class="dropdown-divider"></div>
        <div class="signIn-bottom">
            <a class="signIn-bottom-link" href="#">¿Nuevo por acá? Registrate</a><br>
            <a class="signIn-bottom-link" href="#">¿Olvidaste tu contraseña?</a>
        </div>
    </div>

    <!-- Footer -->
    <%- include('partials/footer') %>
    <!-- Scripts -->
    <%- include('partials/scripts') %>
    <script src="javascript/registerForm.js"></script>   
</body>
</html>